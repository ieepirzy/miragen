{
  "description": "Intentionally empty. Grok Build 1.0.41 does not register plugin hooks at session start (only after a plugin reload), so the Grok hooks live in ~/.grok/hooks/miragen.json, written by `python3 -m miragen_hook install grok-build`. Declaring them here too would fire every event twice once Grok does load plugin hooks.",
  "hooks": {}
}
